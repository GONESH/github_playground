name: My First Github Action
on: [push]
jobs:
    build-app:
        runs-on: ubuntu-latest
        steps:
            - name2: Checkout GitHub repository
            - uses:actions/checkout@v2
             